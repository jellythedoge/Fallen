#region

using Fallen.API;
using hazedumper;
using System.Threading;
using System;

#endregion

namespace Fallen.Features
{
    internal class RCS
    {
        public static void Run()
        {
            var Localplayer = new Entity();

            while (true)
            {
                //MainClass.Memory.ReadMem(LocalPlayer.ClientState + signatures.dwClientState_ViewAngles)
            }
        }
    }
}
